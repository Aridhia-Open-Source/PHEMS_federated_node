.venv
.vscode
artifacts/**
!.keep

*.pyc
*.egg-info
*.gists
*.venv
__pycache__/

.env
*.env
.envrc
k8s/*/*.values.yaml
k8s/*/*.values.yml
*.crt
*.key

# Ingore helm charts archives

**/.coverage
**/coverage.xml
k8s/federated-node/Chart.lock
k8s/federated-node/charts
k8s/federated-node/.debug
debug.yaml

# ... except the tracked example, which the ignore above would otherwise catch
!k8s/*/dev.example.values.yml

# AI
.claude/**

# Ignore local model files
.models/

# temp
federated-node-task-controller
# Use case analysis code lives in its own repo; these are symlinks to a sibling
# checkout (../MVP-Code, ../uc3-dose-interference-model) that the image builds use.
uc1
uc3

# Local use case runs (scripts/run_uc1_local.sh, scripts/run_uc3_local.sh)
uc1-local-results/
uc3-local-results/
