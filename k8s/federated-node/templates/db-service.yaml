{{ if .Values.create_db_deployment }}
apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - port: {{ include "dbPort" . }}
      name: pgport
      targetPort: {{ include "dbPort" . }}
      protocol: TCP
  selector:
    app: postgres
{{ end }}
