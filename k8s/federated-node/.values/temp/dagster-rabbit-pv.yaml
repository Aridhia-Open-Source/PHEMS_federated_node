apiVersion: v1
kind: PersistentVolume
metadata:
  name: dagster-rabbitmq-pv
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: local-hostpath
  persistentVolumeReclaimPolicy: Retain
  local:
    path: /data/dagster/rabbitmq
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - fn-control-plane
