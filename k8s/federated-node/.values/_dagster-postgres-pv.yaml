apiVersion: v1
kind: PersistentVolume
metadata:
  name: dagster-postgres-pv
spec:
  capacity:
    storage: {{ .Values.storage.capacity }}
  accessModes:
    - ReadWriteOnce
  # storageClassName: {{ include "storageClassName" . }}
  persistentVolumeReclaimPolicy: Retain
  local:
    path: {{ .Values.storage.local.dagster_db }}
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - fn-control-plane
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dagster-postgres-pvc
  namespace: {{ .Release.Namespace }}
spec:
  storageClassName: ""
  volumeName: dagster-postgres-pv
  resources:
    requests:
      storage: {{ .Values.storage.capacity }}
  accessModes:
    - ReadWriteOnce

